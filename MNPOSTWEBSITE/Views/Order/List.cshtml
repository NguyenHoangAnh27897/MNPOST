@using PagedList.Mvc;
@using GridMvc.Html;
@model PagedList.IPagedList<MNPOSTWEBSITEMODEL.WS_Mailer> 
@{
    ViewBag.Title = "List";
    Layout = "~/Views/Shared/_LayoutAccount.cshtml";
}
<section id="main-content">
    <section class="wrapper">
        <h3><i class="fa fa-angle-right"></i> Danh sách đơn hàng đã tạo</h3>
        <div class="row">

            <div class="row mt">
                <div class="col-md-12">
                    <div class="content-panel">
                        <table class="table table-striped table-advance table-hover">
                            <h4><i class="fa fa-angle-right"></i> Đơn hàng</h4>
                            <hr>
                            <thead>
                                <tr>
                                    <th><i class="fa fa-bullhorn"></i> Mã đơn hàng</th>
                                    <th><i class="fa fa-question-circle"></i> Tên người gửi</th>
                                    <th><i class="fa fa-bookmark"></i> Tên người nhận</th>
                                    <th><i class="fa fa-edit"></i> Ngày tạo</th>
                                    <th></th>
                                </tr>
                            </thead>
                            <tbody>
                                @foreach (var item in Model)
                                {
                                    <tr>
                                        <td>@item.MailerID</td>
                                        <td class="hidden-phone">@item.SenderName</td>
                                        <td class="hidden-phone">@item.RecieverName</td>
                                        <td>@item.CreationDate</td>
                                        @*<td>
                                           <input type="checkbox" /> Chọn
                                        </td>*@                                   
                                    </tr>
                                }
                            </tbody>
                        </table>
                    <form action="" style="margin-left:10px;">
                        <button class="btn btn-theme" type="submit">Lưu</button>
                    </form>
                    </div><!-- /content-panel -->
                    <br />
                    <strong style="margin: 12px;"> Page @(Model.PageCount < Model.PageNumber ? 0 : Model.PageNumber) of @Model.PageCount</strong>
                    @Html.PagedListPager(Model, page => Url.Action("List", new { page }))
                </div><!-- /col-md-12 -->
            </div><!-- /row -->
        </div>
    </section><! --/wrapper -->
</section><!-- /MAIN CONTENT -->

